import { Navigation } from "@/components";
import styles from "./style.module.scss";

export const Footer = () => {
  return (
    <footer className={styles.footer}>
      <Navigation />
    </footer>
  );
};
